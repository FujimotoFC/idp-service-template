apiVersion: v1
kind: Service
metadata:
  name: REPLACE_SERVICE_NAME
  labels:
    app: REPLACE_SERVICE_NAME
    managed-by: idp
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: REPLACE_SERVICE_NAME
